"""
Data processing utilities for the fair-shares library.

This package contains data processing submodules. Import directly from the
submodule you need rather than from this barrel file:

    from fair_shares.library.utils.data.config import build_data_config
    from fair_shares.library.utils.data.completeness import get_complete_iso3c_timeseries

The parent package (fair_shares.library.utils) re-exports commonly-used
symbols for convenience. This __init__.py no longer re-exports everything;
submodules are still importable via their full paths.
"""
